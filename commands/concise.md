Switch the active concise mode level.

Level: $ARGUMENTS (valid: lite, full, ultra, off)

If $ARGUMENTS is valid, acknowledge with exactly one line: "Concise: $ARGUMENTS"
If $ARGUMENTS is empty, report the current active level.
Do not explain what each level does.
